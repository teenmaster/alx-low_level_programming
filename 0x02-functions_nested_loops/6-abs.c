#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 * @a: integer that is computed
 *
 * Return: absolute value of the integer
 */
int _abs(int a)
{
	if (a < 0)
	{
		a*=-1;
	}

	return (a);
}
